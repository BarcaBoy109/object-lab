# Array and for-each iterations

- [x] Iteration 1 — one-dimensional array fundamentals. Verified with `node --test tests/interpreter.test.js tests/api.test.js` (54 passing after subsequent additions). Commit pending: `.git` is read-only in this environment.
- [ ] Iteration 2 — arrays across OOP features (core parameter/return/covariance behavior implemented; dedicated iteration commit and complete coverage pending).
- [ ] Iteration 3 — enhanced for-each execution (core execution and regression coverage implemented; dedicated iteration commit pending).
- [ ] Iteration 4 — array and loop visualization (heap rendering implemented; browser verification and reference-arrow/highlight refinement pending).
- [ ] Iteration 5 — guided learning examples (array, for-each, aliasing, and polymorphic examples added; remaining example coverage/documentation refinement pending).
- [ ] Iteration 6 — integration and diagnostic hardening.

Known limitations: multidimensional arrays, collections, generics, `Iterable`, streams, and exception handling are unsupported. Array support currently covers one-dimensional declarations, allocation, literals, indexed access, mutation, aliases, `.length`, and historical array snapshots.
